# =============================================================================
# Realtor Site - Environment Variables
# =============================================================================
# This file documents all environment variables required for the application.
# Copy this to .env for local development (NEVER commit .env to version control)
#
# For Cloudflare Pages deployment:
# - Public variables: Set in CF Pages dashboard or wrangler.jsonc "vars" section
# - Secrets: Set using `wrangler secret put VARIABLE_NAME`
# =============================================================================

# -----------------------------------------------------------------------------
# Bridge API Configuration
# -----------------------------------------------------------------------------
# MLS data provider API endpoint and authentication

# Public variable - API base URL (already configured in wrangler.jsonc)
BRIDGE_API_URL=https://api.bridgedataoutput.com

# SECRET - Bridge API access key for MLS data
# Get from: https://bridgedataoutput.com (account dashboard)
# Deploy with: wrangler secret put BRIDGE_API_KEY
BRIDGE_API_KEY=your_bridge_api_key_here

# -----------------------------------------------------------------------------
# Email Service Configuration
# -----------------------------------------------------------------------------
# Resend.com for transactional email notifications

# SECRET - Resend API key for sending lead notification emails
# Get from: https://resend.com/api-keys
# Deploy with: wrangler secret put RESEND_API_KEY
RESEND_API_KEY=your_resend_api_key_here

# -----------------------------------------------------------------------------
# Runtime Environment (Auto-configured by Cloudflare)
# -----------------------------------------------------------------------------
# These are automatically provided by Cloudflare Workers runtime:
# - LEAD_QUEUE: KV namespace binding (configured in wrangler.jsonc)
# - ASSETS: Static assets binding (configured in wrangler.jsonc)

# -----------------------------------------------------------------------------
# Local Development Notes
# -----------------------------------------------------------------------------
# For local development with `npm run dev`:
# 1. Copy this file to .env
# 2. Fill in your actual API keys
# 3. Run `npm run dev` (Astro will load .env automatically)
# 4. For KV testing, use `wrangler dev` with --local flag
#
# SECURITY REMINDER:
# - NEVER commit .env files to version control
# - NEVER expose API keys in client-side code
# - All API calls must go through SSR endpoints
# - Use Astro.locals.runtime.env to access secrets in SSR
# =============================================================================
