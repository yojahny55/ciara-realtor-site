---
/**
 * Root Redirect
 *
 * Redirects visitors from root (/) to the English homepage (/en/)
 * This ensures all pages have explicit language prefixes.
 */

// Redirect to English homepage
return Astro.redirect('/en/', 302);
---
