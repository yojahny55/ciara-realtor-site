---
/**
 * Animated Background Component
 *
 * AC #8: Liquid orbs with glassmorphism background animation
 * - Fixed position, full viewport coverage
 * - 2 orbs with rose gold gradients
 * - Smooth liquid animation (20s ease-in-out infinite)
 * - Respects prefers-reduced-motion
 *
 * Source: demo8 design system
 */
---

<div class="animated-bg">
  <div class="bg-orb bg-orb-1"></div>
  <div class="bg-orb bg-orb-2"></div>
</div>

<style>
  .animated-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
    overflow: hidden;
  }

  .bg-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.7;
    animation: liquidOrbFloat 20s ease-in-out infinite;
  }

  .bg-orb-1 {
    width: 600px;
    height: 600px;
    background: linear-gradient(135deg, #B76E79 0%, #D4A5A5 100%);
    top: -10%;
    right: -10%;
    animation-delay: 0s;
  }

  .bg-orb-2 {
    width: 500px;
    height: 500px;
    background: linear-gradient(135deg, #D4A5A5 0%, #E8C4C4 100%);
    bottom: -5%;
    left: -10%;
    animation-delay: -10s;
  }

  @keyframes liquidOrbFloat {
    0%, 100% {
      transform: translate(0, 0) scale(1) rotate(0deg);
      border-radius: 50% 40% 60% 50%;
    }
    25% {
      transform: translate(30px, -40px) scale(1.05) rotate(5deg);
      border-radius: 60% 50% 40% 55%;
    }
    50% {
      transform: translate(-20px, 20px) scale(0.95) rotate(-3deg);
      border-radius: 45% 55% 50% 45%;
    }
    75% {
      transform: translate(15px, 30px) scale(1.02) rotate(2deg);
      border-radius: 55% 45% 55% 50%;
    }
  }

  /* AC #8: Respect prefers-reduced-motion */
  @media (prefers-reduced-motion: reduce) {
    .bg-orb {
      animation: none;
    }
  }
</style>
